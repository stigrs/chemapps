parameters:
  jobName: ''
  imageName: ''

jobs:
- job:
  displayName: ${{ parameters.imageName }}
  pool:
    vmImage: ${{ parameters.imageName }}
  strategy:
    matrix:
      debug:
        BUILD_TYPE: 'Debug'
      release:
        BUILD_TYPE: 'Release'
  continueOnError: false

  steps:
  - powershell: |
      Invoke-WebRequest -Uri "https://boostorg.jfrog.io/artifactory/main/release/1.77.0/source/boost_1_77_0.7z" -OutFile boost_1_77_0.7z
      7z x boost_1_77_0.7z
      dir .\boost_1_77_0
    name: Prepare

#  - task: CMake@1
#    name: Configure
#    inputs:
#      workingDirectory: build
#      cmakeArgs: ' .. '
#
#  - task: CMake@1
#    name: Build
#    inputs:
#      workingDirectory: build
#      cmakeArgs: '--build . --config $(BUILD_TYPE)'
#
#  - script: ctest -C $(BUILD_TYPE) --output-on-failure --no-compress-output
#    name: CTest
#    workingDirectory: build
#    failOnStderr: true